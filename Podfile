# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target '在庫管理' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Firebase/Core'
  pod 'FirebaseUI'

  # Pods for 在庫管理

  target '在庫管理Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target '在庫管理UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
